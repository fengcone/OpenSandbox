apiVersion: sandbox.opensandbox.io/v1alpha1
kind: BatchSandbox
metadata:
  name: {{.BatchSandboxName}}
  namespace: {{.Namespace}}
spec:
  replicas: {{.Replicas}}
  poolRef: {{.PoolName}}
{{- if .ExpireTime }}
  expireTime: "{{.ExpireTime}}"
{{- end }}